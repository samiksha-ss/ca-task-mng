import { FeaturePlaceholder } from "@/components/layout/feature-placeholder";

export default function CompaniesPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Client records"
      title="Companies"
      description="This area will manage client company context, statuses, and priority tiers for accounting work."
    />
  );
}
