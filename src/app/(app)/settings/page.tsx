import { FeaturePlaceholder } from "@/components/layout/feature-placeholder";

export default function SettingsPage() {
  return (
    <FeaturePlaceholder
      eyebrow="Configuration"
      title="Settings"
      description="Personal preferences and workspace-level configuration will be added in later steps."
    />
  );
}
